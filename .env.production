# Solana Token Creator Lookup Service - Production Configuration

# Server
PORT=3001
NODE_ENV=production

# RPC Endpoints (use your own for production)
# Primary RPC providers: Helius, QuickNode, or Alchemy
SOLANA_RPC_1=https://api.mainnet-beta.solana.com
SOLANA_RPC_2=https://api.mainnet-beta.solana.com
SOLANA_RPC_3=https://api.mainnet-beta.solana.com

# Cache Configuration
CACHE_TTL=300000          # 5 minutes (in milliseconds)
MAX_CACHE_SIZE=10000      # Maximum tokens to store in cache

# Rate Limiting (per IP)
RATE_LIMIT_WINDOW=60000   # 1 minute (in milliseconds)
RATE_LIMIT_MAX_REQUESTS=100  # Max requests per IP per window

# Request Queue
MAX_QUEUE_SIZE=5000       # Maximum pending requests
MAX_CONCURRENT_REQUESTS=50  # Max concurrent Solana RPC calls

# CORS
CORS_ORIGIN=*             # Update to your domain in production

# Monitoring (optional)
LOG_LEVEL=info
LOG_FILE=/var/log/betasonchain/server.log

# Performance
NODE_OPTIONS=--max-old-space-size=4096  # 4GB heap for 1000+ users
